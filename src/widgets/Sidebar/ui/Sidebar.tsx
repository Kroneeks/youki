import { VStack } from '@/shared/ui/Stack';
import { memo } from 'react';

const Sidebar = memo(() => {
    return <VStack>Sidebar</VStack>;
});

Sidebar.displayName = 'Sidebar';

export { Sidebar };
